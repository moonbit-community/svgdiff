# Structured Report Prototype

**PROTOTYPE — throw this code away or absorb the validated model before production implementation.**

Run the complete scenario suite with one command:

```sh
moon test prototype/structured_report --target native
```

## Question

Can the accepted layered model produce a machine-readable Structured Report for representative equivalent, subtle, salient, resource-mediated, zero-contribution, unsupported, and JSON-serialization cases using the current MoonBit SVG ecosystem?

The package exposes the conceptual seam from the core model:

```text
compare(before_svg, after_svg, comparison_profile) -> structured_report
```

This is fixture-specific validation code, not a general SVG diff implementation. Read [`NOTES.md`](NOTES.md) for the verdict and limitations.
