# M2 Alignment, Region, and Cause Soundness Gate

`gate.v1.json` records four independent obligation groups:

- Subject Alignment correspondence and uncertainty;
- side-typed Difference Region localization;
- complete Cause Envelope containment and conservative fallback; and
- closed Structured Report references.

Run:

```sh
sh scripts/test-m2-soundness-gate.sh
```

The gate combines focused MoonBit properties with deterministic mutation, curated annotation, and adversarial suites. It proves the declared supported boundary, not authoritative identity, exact pixel contribution, minimal causes, or behavior hidden behind a partial Diagnostic.
