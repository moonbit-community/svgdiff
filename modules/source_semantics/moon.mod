name = "username/svgdiff_source_semantics"

version = "0.1.0"

readme = "README.mbt.md"

repository = ""

license = "Apache-2.0"

keywords = [ "svg", "xml", "parser", "source-semantics" ]

preferred_target = "wasm-gc"

description = "Strict source-semantics parser with provenance spans for SVG Diff"
