# ISS-020: Promote the comparison engine out of prototype

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: architecture, packaging, docs, tests, agent
- Assignee: codex
- Created: 2026-07-14
- Updated: 2026-07-14
- External ref: none

## Description
The production comparison implementation still lives in `prototype/structured_report`, and the root package re-exports types whose package identity exposes that experimental path. Promote the implementation to a formal engine package and remove stale claims that the active engine is throwaway prototype code.

## Design
Move the package unchanged to `engine`, preserve the root package as the stable product seam, update package imports and current documentation, and use schema version `1.0` throughout the formal engine. Keep the genuine influence-provenance renderer spike under `prototype` and preserve historical issue notes.

## Acceptance Criteria
- [x] No active package or generated public interface references `prototype/structured_report`.
- [x] Formal engine reports use schema version `1.0` rather than a prototype schema marker.
- [x] Current documentation describes `engine` as production code and keeps only genuine spikes under `prototype`.
- [x] `moon info`, formatting, warning checks, all native tests, and CLI integration pass.

## Relationships
- Depends on: ISS-004
- Parent: none
- Related: ISS-011
- Discovered from: post-epic productization review

## Notes
- 2026-07-14: The behavior is complete; this task closes the remaining package and interface productization gap.

## Close Notes
Moved the complete comparison package to `engine`, updated black-box package references and the root import, regenerated interfaces so public type identities reference `username/svgdiff/engine`, and promoted every active report path to schema `1.0`. Replaced the obsolete throwaway README with formal engine documentation and archived the original verdict under `docs/research`. Verified 72 native tests, warning checks, CLI integration, JSON Schema parsing, and the absence of active prototype package references.
