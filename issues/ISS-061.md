# ISS-061: Enforce conservative causal fallback

## Metadata
- Type: task
- Status: closed
- Priority: 3
- Labels: agent, causality, coverage, provenance, roadmap, tests
- Assignee: codex
- Created: 2026-07-14
- Updated: 2026-07-14
- External ref: none

## Description
Reconcile the already implemented Cause Envelope fallback with the Phase 6 roadmap and strengthen the executable guarantee that missing propagation coverage can never produce a falsely precise complete envelope.

## Design
Retain the implemented safe rule: complete reports may use only established event-region independence; an empty established candidate set widens to every Changed Fact. Any partial report revokes `sound_overapproximation`, includes every Changed Fact for each available region, records all Diagnostics, and uses comparison fallback scope. Correct stale current-contract JSON examples to the actual schema `1.0` shape.

## Acceptance Criteria
- [x] Complete regions with established independence retain their sound event-region candidates.
- [x] A partial comparison with multiple Changed Facts includes the complete fact universe in every available Cause Envelope.
- [x] Partial envelopes use `not_established`, `partial`, comparison scope, and establishing Diagnostics.
- [x] Canonical production-report validation enforces the fallback and guarantee fields.
- [x] Current causal documentation matches the exact schema `1.0` field names and enum spellings.
- [x] No unsupported or unavailable propagation path can retain `sound_overapproximation` with a pruned candidate set.

## Relationships
- Depends on: ISS-009, ISS-040, ISS-056, ISS-058
- Parent: none
- Related: ISS-016, ISS-029
- Discovered from: roadmap Phase 6 fallback or guarantee revocation

## Notes
- ISS-009 implemented the safe fallback. This item adds multi-fact and production-report gates and repairs stale design-era JSON in the current contract.

## Close Notes
Retained the ISS-009 implementation rule and added a two-event, two-fact partial regression proving every available region widens to the complete Changed Fact universe. Each envelope uses `not_established`, `partial`, comparison fallback scope, and nonempty establishing Diagnostics. Existing complete disjoint-event coverage still proves established independence may retain precise event-region candidates.

Extended the production determinism validator to enforce causal guarantee consistency. A sound envelope is permitted only on a complete report; a revoked envelope must be partial, comparison-scoped, Diagnostic-backed, and contain every Changed Fact. A negative control deletes one valid candidate from a partial gradient envelope and proves the gate rejects the resulting false precision.

Corrected the current influence-provenance document's design-era JSON to schema `1.0`: underscore enum spellings, Changed Fact candidate IDs, current fallback scopes, and no unimplemented granularity or influence-path fields.

Verified generated interfaces and formatting, warning-clean native checks, all 103 MoonBit tests, canonical Schema examples, report determinism and causal negative controls, actual-cause annotations, the report-only Agent harness, reproducible benchmark pass/fail behavior, Python and shell syntax, local Markdown links, and `git diff --check`.
