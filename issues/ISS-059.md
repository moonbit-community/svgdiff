# ISS-059: Reconcile explicit coverage summaries

## Metadata
- Type: task
- Status: closed
- Priority: 3
- Labels: agent, coverage, roadmap, schema, tests
- Assignee: codex
- Created: 2026-07-14
- Updated: 2026-07-14
- External ref: none

## Description
Confirm that the existing `coverage_matrix` is the canonical explicit summary by subject, feature, and evidence layer, and add production-report validation for its structural and status invariants.

## Design
Do not add a redundant aggregate object beside `coverage_matrix`. Validate canonical production reports for nonempty feature and subject keys, unique and deterministic row keys, valid independent layer states, Diagnostic closure for constrained cells, and exact derivation of `analysis_status`. Retain existing MoonBit complete, partial, and failed integration coverage.

## Acceptance Criteria
- [x] Current documentation names `coverage_matrix` as the sole canonical coverage summary.
- [x] Every validated row contains a nonempty feature ID, subject ID, and all three evidence-layer states.
- [x] Row keys are unique and sorted deterministically by feature ID then subject ID.
- [x] Every limited or failed cell is justified by a referenced Diagnostic affecting that layer.
- [x] Top-level `analysis_status` exactly summarizes the strongest matrix cell.
- [x] Production-report validation covers complete and partial examples, while MoonBit integration tests retain failed-report coverage.
- [x] Agent guidance reads the matrix directly rather than deriving coverage from difference counts.

## Relationships
- Depends on: ISS-039, ISS-040, ISS-056, ISS-058
- Parent: none
- Related: ISS-024, ISS-041
- Discovered from: roadmap Phase 7 explicit coverage summaries

## Notes
- `FeatureCoverage` and the production matrix were implemented by ISS-039. This item closes the duplicate roadmap wording with stronger end-to-end validation rather than a second report model.

## Close Notes
Confirmed that the existing `coverage_matrix` is already the complete subject-by-feature-by-evidence-layer summary introduced by ISS-039, so no redundant aggregate report field was added. Documented that agents must read, group, or filter this matrix directly instead of inferring coverage from differences, events, or Diagnostic code names.

Extended canonical production-report validation to require a nonempty matrix; nonempty feature and subject keys; all Source Semantics, Computed Appearance, and Rendered Evidence states; unique feature/subject row keys; exact MoonBit shortlex row order; Diagnostic closure for every limited or failed cell; and exact derivation of top-level `analysis_status`. The existing MoonBit integration tests retain complete, partial, and failed report coverage plus final proof-obligation enforcement.

The validation initially exposed that Python lexical order differs from MoonBit `String::compare`. The latter intentionally uses shortlex order, so the validator now reproduces that documented current contract rather than changing deterministic production output.

Verified generated interfaces and formatting, warning-clean native checks, all 102 MoonBit tests, seven canonical production reports twice, report determinism and reference closure, the report-only Agent harness, Python syntax, local Markdown links, and `git diff --check`.
