# ISS-184: Implement the event-centered production HTML report

## Metadata
- Type: feature
- Status: closed
- Priority: 1
- Labels: html, ux, interaction, tests
- Assignee: Milky2018
- Created: 2026-07-18
- Updated: 2026-07-18
- External ref: none

## Description

Replace the domain-first Atomic Difference sidebar with the accepted Visual Event-centered production report while preserving Structured Report as the sole semantic evidence source and keeping sandboxed SVG previews self-contained.

## Design

Use the accepted evidence-rail desktop structure: synchronized previews on the left and compact overview, visibility filters, Rendered Outcome Groups, Event summaries, complete Atomic inventories, owned evidence disclosures, Diagnostics, and Raw Report on the right. Project legacy Schema 1.44 facts conservatively; do not add the future localization or Measurement Availability schema fields in this task.

## Acceptance Criteria

- [x] All criteria recorded in ISS-183 pass against generated production HTML.
- [x] Existing CLI HTML generation, CSP isolation, and Structured Report JSON embedding remain compatible.
- [x] `moon check --target native --warn-list +73`, targeted/full tests, `moon info`, `moon fmt`, and `git diff --check` pass.

## Relationships
- Depends on: ISS-183
- Parent: ISS-175
- Related: ISS-012, ISS-143
- Discovered from: ISS-175

## Notes

- 2026-07-18: Claimed after the user explicitly chose production implementation instead of further disposable prototyping.

## Close Notes

Implemented the production event-centered HTML projection in `html_report.mbt` and `html_report_assets.mbt`. The report now provides outcome-group navigation, compact overview and Impact Map, Event summaries, default-visible complete Atomic inventories, owned evidence disclosures, hierarchical session-only review marks, visibility-only filters, persistent and temporary Event localization, explicit Schema 1.44 bounds degradation, synchronized zoom/pan/fit controls, and complete Raw Report JSON. The sandbox viewport now scales SVGs without `viewBox` while preserving hostile-source isolation.

Verified with `moon test --target native`, `sh scripts/test-cli.sh`, `sh scripts/test-html-security.sh`, `moon check --target native --warn-list +73`, `moon info --target native`, `moon fmt`, and `git diff --check`. Browser acceptance covers representation-only measured-zero color spelling, tiny multidimensional geometry, obvious rendered change, partial evidence, tied/incomparable Impact, empty/failed reports, hierarchy controls, filters, selection, viewport synchronization, accessibility labels, and hostile SVG isolation.
