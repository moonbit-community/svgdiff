# ISS-022: Rename the CLI package to cmd/svgdiff

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: cli, packaging, docs, generated-interface, agent
- Assignee: codex
- Created: 2026-07-14
- Updated: 2026-07-14
- External ref: none

## Description
Move the native executable package from its former generic path to the product-named `cmd/svgdiff` path and keep every documented and tested invocation synchronized.

## Design
Rename the package directory without changing CLI behavior, update active repository references, regenerate the package interface with `moon info`, and validate the new invocation path end to end.

## Acceptance Criteria
- [x] The executable package lives at `cmd/svgdiff`.
- [x] No tracked file references the former generic executable path.
- [x] `moon run --target native cmd/svgdiff` is the documented and tested invocation.
- [x] Formatting, warning checks, all native tests, and CLI integration pass.

## Relationships
- Depends on: ISS-021
- Parent: none
- Related: ISS-011
- Discovered from: CLI package productization

## Notes
- 2026-07-14: Created for the requested binary package path rename.

## Close Notes
Moved the executable package to `cmd/svgdiff`, updated README and integration-script invocations, and regenerated its package interface. Added an explicit `@lib` alias for the root library dependency to avoid colliding with the executable package's own `svgdiff` alias. Verified warning checks, all 72 native tests, CLI integration, JSON Schema parsing, and zero residual references to the former path.
