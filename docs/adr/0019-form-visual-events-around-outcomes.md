# Form Visual Events around outcomes

Status: superseded in part by ADR-0040

Visual Event boundaries will follow coherent visual outcomes rather than shared causes alone. One resource or source-level Atomic Difference may therefore be referenced by multiple Events when it produces independent outcomes, but the Atomic Difference remains stored once; this keeps Events useful as agent navigation and localization units without losing the shared candidate causes recorded by Cause Envelopes.
