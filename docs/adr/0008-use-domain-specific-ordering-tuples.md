# Use Domain-specific ordering tuples

Differences in the same Domain will be ordered by a versioned lexicographic tuple of Domain-relevant measurements rather than a universal normalized score. Geometry, paint, text, compositing, resource, document, and presence changes have different physically and perceptually meaningful units; preserving those units and declaring an ordering tuple keeps ranking explainable without pretending that unrelated magnitudes are directly interchangeable.
