# Record differences at three evidence layers

Every comparison will distinguish Source Semantics, Computed Appearance, and Rendered Evidence instead of collapsing them into a single equality result. This preserves authored distinctions such as `red` versus `#ff0000`, identifies computed changes too small to affect a rendering, and separately measures changes that materially alter the visible result.
