# Roadmap Review and Rejection Ledger

Status: current maintenance policy

Last reviewed: 2026-07-17

This document prevents roadmap cleanup from erasing product intent. It defines when [`roadmap.md`](../roadmap.md) must be reviewed and records product shapes that the project has intentionally rejected.

## State distinctions

An unchecked roadmap item remains part of the backlog unless a review explicitly changes its state.

| State | Meaning | Required record |
| --- | --- | --- |
| Planned | Accepted direction, not necessarily scheduled | Keep the checklist item and its priority. |
| Deferred | Valuable only after named prerequisites or a later milestone | Keep the checklist item with `Deferred` and the prerequisite. |
| Decision | Product or architecture choice is still open | Keep the checklist item with `Decision`; resolve it through an ADR before implementation. |
| Rejected | The project intentionally will not provide the capability or product shape in the named scope | Remove it from active milestone gates only in the same commit that adds or updates a rejection-ledger entry. |
| Superseded | A different accepted capability satisfies the original outcome | Link the replacement and its decision; do not silently delete the original intent. |

Unsupported current behavior is not automatically rejected. For example, deterministic fonts, animation comparison, `foreignObject`, and cross-browser profiles remain explicitly recorded future decisions or deferred work.

## Milestone review procedure

Review the roadmap when a milestone begins, when its acceptance gate is claimed complete, and whenever the terminal product goal or milestone scope changes materially.

For each review:

1. Re-read the terminal goal, non-negotiable correctness rules, phase checklist, and milestone gate.
2. Confirm that completed items have verification evidence in their issue or commit and mark them complete.
3. Keep unscheduled work visible as planned, deferred, or decision work; update prerequisites when they change.
4. For every removed or narrowed capability, add a rejection or supersession entry below with its exact scope, evidence, and reconsideration trigger.
5. Update `roadmap.md`'s `Last reviewed` date and append one row to its review log in the same commit.

A roadmap review may conclude that no capability should move or be removed. That result still belongs in the review log.

## Rejection ledger

These entries describe exact rejected forms. They do not reject adjacent capabilities that remain in the roadmap.

| Rejected form | Scope | Reason and accepted replacement | Decision evidence | Reconsideration trigger |
| --- | --- | --- | --- | --- |
| Pure XML formatting variation as a Visual Difference, or raw XML text diff as the primary model | All report versions | Attribute order, quote style, tag closing, entity spelling, and declaration whitespace are not visual-semantic outcomes. Compare normalized Declared Visual Facts while retaining reportable authored values and Source Spans as provenance. | [ADR 0027](adr/0027-represent-authored-visual-declarations-as-facts.md) | A future independent source-audit projection may expose textual edits, but it cannot enter Visual Events or replace the visual-semantic model. |
| A visibility boolean or universal scalar as the source of truth | All report versions | Incommensurate exact, geometric, raster, coverage, color, and perceptual measurements must remain available. Optional labels or rankings require a versioned policy. | [ADR 0007](adr/0007-separate-magnitude-evidence-from-impact-policy.md) | None for the canonical evidence model; new derived policies may be added without deleting measurements. |
| Pixel-only comparison as proof of equality or the sole magnitude oracle | All complete analyses | A raster cannot explain authored equivalence or subpixel computed changes, and it cannot establish causal attribution by itself. Preserve Source Semantics, Computed Appearance, and Rendered Evidence separately. | [ADR 0004](adr/0004-record-differences-at-three-evidence-layers.md), [ADR 0006](adr/0006-use-render-observations-as-visible-change-evidence.md) | None for complete analysis; additional renderers may add evidence under explicit profiles. |
| Implicit network resources, animation state, or platform state inside the deterministic static profile | Deterministic static profile only | Undeclared state makes comparison irreproducible. Encountering it reduces coverage; accepted future timeline, resource-snapshot, and observation boundaries remain separately identified and unimplemented. | [ADR 0009](adr/0009-scope-initial-correctness-to-deterministic-static-svg.md), [ADR 0104](adr/0104-compare-animation-at-explicit-logical-checkpoints.md), [ADR 0106](adr/0106-use-side-qualified-resource-response-snapshots.md) | A separate versioned profile closes every input and passes its implementation gates; implicit acquisition still cannot enter comparison. |
| SVG script execution in canonical analysis, or treating browser sandbox/CSP as deterministic execution | All canonical Comparison Profiles | Script enables the DOM, event loop, Web APIs, dynamic resources, and ambient state. Preserve secure-static analysis and source-located coverage limits; permit only a separately versioned target-local external observation after explicit closure and replay gates. | [ADR 0102](adr/0102-keep-svg-script-execution-outside-canonical-analysis.md), [script execution boundary](script-execution-boundary.md) | A concrete Agent task cannot be served by static reports plus observations, and a separate proposal closes language, DOM, event, timeline, resources, scheduling, state, limits, cross-host replay, and complete causal provenance. |
| Arbitrary pseudo-class match booleans, or treating a browser action trace as canonical interaction state | Future interaction profiles | Dynamic matches depend on URL, focus, modality, devices, hit testing, ancestry, state feedback, and achieved postconditions. Derive matches from typed checkpoint seeds; keep action replay as a separately versioned target-local observation. | [ADR 0103](adr/0103-separate-declared-interaction-state-from-action-observations.md), [interaction-state boundary](interaction-state-profiles.md) | A narrower state family proves that direct declaration preserves every normative invariant and improves an Agent task better than typed seeds plus derived state; action observations still cannot become canonical by intent alone. |
| Wall-clock waits or frame counts as animation time, progress normalization as temporal equality, or finite samples as interval proof | Future animation profiles and observations | These shortcuts erase inactive or unresolved time, duration and schedule edits, event and transition history, and unsampled behavior. Use exact typed logical checkpoints, preserve mechanism state, and require complete analytic/event-partition evidence for interval equality. | [ADR 0104](adr/0104-compare-animation-at-explicit-logical-checkpoints.md), [animation timeline boundary](animation-timeline-model.md) | A narrower mechanism proves a complete deterministic relation under a versioned logical-time profile; browser delay and finite sampling still cannot gain broader authority. |
| XML-only or outer-rectangle `foreignObject` comparison, unknown-namespace fallback, or browser pixels as canonical foreign-content semantics | Future foreign-content profiles and observations | SVG geometry does not define descendant host-language styling, layout, fonts, resources, paint, or integration. Require a deterministic handler for each namespace, permit only closed subset claims, and keep browser captures target-local. | [ADR 0105](adr/0105-require-a-deterministic-host-language-engine-for-foreign-content.md), [foreign-content boundary](foreign-object-layout-boundary.md) | A concrete Agent task and versioned closed host-language slice satisfy semantic, layout, resource, security, provenance, conformance, and limit gates; a wider claim still requires wider engine coverage. |
| Generalizing current opaque resource keys in place, `URL -> bytes` maps, one shared before/after snapshot, or comparison-time fetching | Future external resource profiles | These forms erase base/request/response/failure distinctions or import mutable network, filesystem, credential, cache, and timing state. Use side-qualified offline response snapshots with typed request identity, recursive closure, independent interpretation, and separate prefetch provenance. | [ADR 0106](adr/0106-use-side-qualified-resource-response-snapshots.md), [resource snapshot boundary](general-resource-snapshot-bundles.md) | A concrete Agent task and versioned offline format, URL policy, family slice, closure proof, security limits, and evaluation pass; comparison-time I/O remains a permanent non-goal. |
| Persistent private-intermediate, graph-incremental, or shared comparison caching without measured demand and complete invalidation | Current product and future cache proposals | Pair-global alignment, regions, events, and provenance can change after a local edit, while private intermediates have no stable schema. Keep full recomputation authoritative and permit only measured within-call reuse; reserve an optional local exact-result cache as the first future candidate. | [ADR 0107](adr/0107-defer-persistent-caching-until-measured.md), [cache investigation](incremental-cache-investigation.md) | A representative repeated-workload study proves value and a versioned key/envelope, cold-hit equivalence, corruption/security, limits, cancellation, upgrade, and fallback gates pass. Shared reuse additionally requires an authenticated trust model. |
| Alternate-scale diagnostic rerenders in canonical Difference Magnitude | Canonical Structured Report | Magnified samples can exaggerate tiny changes and duplicate continuous computed geometry evidence. They remain renderer-conformance QA. | [ADR 0013](adr/0013-keep-diagnostic-rerendering-outside-the-report.md) | A separately versioned diagnostic artifact is justified without changing canonical magnitude semantics. |
| Production Impact tiers or a total event order calibrated from the single-pass v1 corpus | Schema `1.43` reports and the current thirteen-case evaluation corpus | The corpus has no recorded independent reviewer agreement, only one `low` event, one `high` target without policy inputs, and no ranked multi-event pair. Retain the traceable uncalibrated `event_rendered_pareto/v1` frontier and its raw inputs. | [ADR 0089](adr/0089-reject-impact-calibration-from-the-v1-corpus.md), [calibration results](../evaluation/impact-calibration/results.v1.json) | Independent blinded agreement, ranked multi-event cases, broader tier and boundary coverage, complete policy inputs, and profile sensitivity evidence are versioned and pass the declared release gates. |
| SSIM or MS-SSIM as canonical report evidence, equality, severity, localization, or Impact semantics | Current deterministic static profile and Schema `1.43` | Whole-canvas values are scale-sensitive, coverage-dependent, non-localizing, and misordered against current human tiers. Retain them only as versioned QA observations beside authoritative raw, region, cause, DeltaEOK, and FLIP evidence. | [ADR 0090](adr/0090-keep-ssim-out-of-canonical-report-semantics.md), [diagnostic results](../evaluation/ssim-diagnostics/results.v1.json) | A new Agent task cannot be answered by existing evidence and an accepted proposal demonstrates canonical-scale meaning, complete renderer coverage, localization links, and independent human validation. |
| LPIPS as canonical report evidence, equality, severity, localization, Impact semantics, a default dependency, or a release gate | Current deterministic static profile and Schema `5.0` | The learned scalar requires alternate-scale RGB input, an explicit background, a Python neural runtime, calibration and backbone weights, and a fixed platform. The experiment is scale/background sensitive, inherits renderer false identity, and misorders current tiers. Retain only the pinned optional offline artifact beside authoritative exact, raw, DeltaEOK, FLIP, region, and cause evidence. | [ADR 0096](adr/0096-keep-lpips-outside-product-semantics.md), [experiment results](../evaluation/lpips-experiment/results.v1.json) | A new Agent task cannot be answered by existing evidence and an accepted proposal demonstrates canonical-scale meaning, complete renderer coverage, event links, independent human validation, portable deterministic execution, and cleared model distribution obligations. |
| Semantic priority inferred from SVG source wording, authored IDs, visual magnitude, or frontier membership | Context-free Impact and Schema `1.43` | Business meaning is caller context, not an intrinsic visual measurement. Preserve every event and apply caller concerns as external queries over report evidence; unresolved concern identity remains unknown. | [ADR 0091](adr/0091-separate-query-concern-from-context-free-impact.md), [semantic-concern results](../evaluation/semantic-concern/results.v1.json) | A versioned deterministic Concern Profile is independently evaluated and proves reliable semantic matching without suppressing the complete inventory. |
| Platform-native font rendering as canonical execution, or ambient system-font output as complete evidence | All deterministic Font Execution Profiles | CoreText, DirectWrite, GDI, browsers, and system font stacks retain OS, framework, device, user, locale, fallback, and hidden state. Use the pinned project runtime for future canonical evidence and keep exact-bundle native output as an external Platform Font Observation; ambient output is exploratory only. | [ADR 0099](adr/0099-keep-platform-font-rendering-observational.md), [platform font boundary](platform-native-font-modes.md) | A concrete Agent task cannot be served by canonical evidence plus observations, and a named native target exposes closed resources, complete inputs, stable identity, hard limits, evidence layers, and exact repeated/cross-host conformance under a new target-specific profile. |
| A generic wide-gamut/HDR flag, ambient display profile, implicit platform CMS, silent sRGB retag/clipping, or direct equality across different color profiles | All deterministic Color Execution Profiles | Color results depend on source space, transforms, interpolation, compositing, reference output, mapping, measurement, resources, and implementation. Stage predefined SDR, ICC, and HDR separately; keep physical display output observational and require the same complete profile for before and after. | [ADR 0100](adr/0100-stage-color-management-beyond-srgb.md), [color-management boundary](color-management-profiles.md) | A concrete `svgdiff-color-execution-profile/1` instance closes every required layer, passes hostile-input and cross-target conformance gates, and demonstrates compatible magnitude semantics without importing ambient state. |
| One generic renderer diff, renderer-majority truth, or direct attribution from an engine-plus-profile diagonal | Future multi-renderer experiments | Same-engine profile sensitivity and same-profile renderer conformance answer different questions. Preserve typed cells and one-axis edges, keep disagreements and failures, and mark undecomposed diagonals confounded. | [ADR 0101](adr/0101-model-multi-renderer-work-as-typed-experiments.md), [multi-renderer boundary](multi-renderer-profiles.md) | A normative authority or project-owned mathematical reference defines a versioned hierarchy for one named feature; it still cannot create an untyped vote or erase incompatible evidence. |

## Baseline review

The 2026-07-14 pre-M1 review retained every unchecked roadmap capability. No planned, deferred, or decision item was removed. The 2026-07-16 Impact review completed the current-corpus study while preserving recalibration as explicit deferred work and leaving the M3 calibrated-Impact gate open. The 2026-07-17 font-profile review rejected native stacks only for canonical execution while retaining exact-bundle observations and the separately declared Phase 9 platform-observation work. The 2026-07-17 color-profile review retained predefined SDR as the first future candidate, staged ICC and HDR separately, and rejected only implicit or ambient execution forms. The 2026-07-17 multi-renderer review retained both cross-profile and cross-target questions as typed future experiments while rejecting generic edges, majority truth, and diagonal attribution. The 2026-07-17 script review permanently retained secure-static canonical execution and moved any future script-enabled output behind a separately versioned external-observation gate. The 2026-07-17 interaction review retained future canonical state checkpoints and external browser reproduction as separate artifacts while rejecting boolean match bags and action-intent authority.
